🚀 SAFEHER FRONTEND - QUICK START GUIDE
═════════════════════════════════════════════════════════════════════

✅ FRONTEND CREATED SUCCESSFULLY!


📁 FOLDER STRUCTURE
═════════════════════════════════════════════════════════════════════

frontend/
├── src/
│   ├── components/          React components (reusable)
│   ├── pages/              Page views (Login, Dashboard)
│   ├── services/           API client (api.ts)
│   ├── styles/             CSS styles (Tailwind)
│   ├── App.tsx             Main application component
│   └── main.tsx            Entry point
├── public/                  Static assets
├── package.json             Dependencies
├── tsconfig.json           TypeScript config
├── vite.config.ts          Vite config
├── tailwind.config.js      Tailwind CSS config
└── README.md               Full documentation


🎯 WHAT'S INCLUDED
═════════════════════════════════════════════════════════════════════

✅ LOGIN PAGE
   • Register new users
   • Login with username
   • Email and PIN setup

✅ DASHBOARD
   • Real-time location analysis
   • Safety risk assessment
   • Emergency contacts management
   • Location history tracking

✅ FEATURES
   • SOS emergency button
   • Location-based safety analysis
   • Contact management
   • Location history

✅ TECH STACK
   • React 18
   • TypeScript
   • Vite (fast build tool)
   • Tailwind CSS (styling)
   • Axios (HTTP client)
   • Lucide React (icons)


🚀 SETUP INSTRUCTIONS
═════════════════════════════════════════════════════════════════════

STEP 1: Navigate to frontend folder
   cd frontend

STEP 2: Install dependencies
   npm install
   (This will download all required packages from package.json)

STEP 3: Create .env file
   Copy .env.example to .env:
   - On Windows (PowerShell): copy .env.example .env
   - Or manually create .env with:
     VITE_API_URL=http://localhost:5000
     VITE_APP_NAME=SAFEHER

STEP 4: Start the backend (in separate terminal)
   cd ..
   python app.py
   (Make sure backend is running on localhost:5000)

STEP 5: Start the frontend development server
   npm run dev
   (App will run on http://localhost:3000)


🔌 MAKE SURE BACKEND IS RUNNING
═════════════════════════════════════════════════════════════════════

Before starting frontend:
1. Open a terminal in the SAFEHER directory (parent folder)
2. Run: python app.py
3. Check: GET http://localhost:5000/ping should return {"status": "ok"}


🎨 FRONTEND PAGES
═════════════════════════════════════════════════════════════════════

LOGIN PAGE
├─ Register: Create new account with username, password, email, PIN
├─ Login: Use existing username to access dashboard
└─ Error handling: Shows validation errors clearly

DASHBOARD
├─ Safety Check Tab
│  ├─ Current location
│  └─ Risk level (Low/Medium/High)
├─ Contacts Tab
│  └─ List of emergency contacts
└─ Location History Tab
   └─ Past location records with timestamps

HEADER
├─ App title "SAFEHER"
├─ Current username
└─ Logout button

SOS BUTTON
└─ Red emergency button on dashboard (triggers SOS alert)


🌐 API INTEGRATION
═════════════════════════════════════════════════════════════════════

The frontend connects to these backend endpoints:

✅ Authentication
   POST /register          Register new user
   GET /user/<username>    Get user details

✅ Contacts
   POST /contact           Add emergency contact
   GET /contacts/<username> Get all contacts

✅ Location & Safety
   POST /analyze           Analyze location safety
   GET /locations/<username> Get location history

✅ Emergency
   POST /sos               Trigger emergency alert

✅ Health
   GET /ping               Check backend status


💻 DEVELOPMENT
═════════════════════════════════════════════════════════════════════

Available commands:
   npm run dev             Start development server (localhost:3000)
   npm run build           Build for production
   npm run preview         Preview production build
   npm run lint            Run code linter

Hot Reload: Changes update instantly in browser (no refresh needed)


📱 BROWSER SUPPORT
═════════════════════════════════════════════════════════════════════

✅ Desktop
   • Chrome 90+
   • Firefox 88+
   • Safari 14+
   • Edge 90+

✅ Mobile
   • iOS Safari 13+
   • Chrome Android
   • Samsung Internet


🔐 IMPORTANT NOTES
═════════════════════════════════════════════════════════════════════

✅ Location Permission
   • App will ask for location permission
   • Required for safety checks and SOS
   • HTTPS needed for production (HTTP works locally)

✅ Session Management
   • Username saved in localStorage
   • Auto-login on page refresh
   • Logout clears session

✅ Production Checklist
   • Use HTTPS (not HTTP)
   • Set proper backend URL in .env
   • Add authentication tokens (JWT)
   • Enable SSL certificate


📂 FILE DESCRIPTIONS
═════════════════════════════════════════════════════════════════════

src/App.tsx
   Main app component, handles login/logout state

src/main.tsx
   React entry point, mounts App to DOM

src/pages/LoginPage.tsx
   Login and registration UI

src/pages/Dashboard.tsx
   Main dashboard with Safety, Contacts, and History tabs

src/services/api.ts
   All API calls to backend (axios wrapper)

src/styles/index.css
   Global styles with Tailwind CSS

vite.config.ts
   Build configuration (Vite)

tsconfig.json
   TypeScript compiler options

tailwind.config.js
   Tailwind CSS theme configuration


🧪 TESTING
═════════════════════════════════════════════════════════════════════

Test Account (after running backend):
   Username: testuser
   Password: test123
   Email: test@example.com
   PIN: 1234

Register this test account:
1. Start backend: python app.py
2. Start frontend: npm run dev
3. Click "Don't have an account? Register"
4. Enter the test account details
5. Click Register
6. Login with testuser / test123


🚨 TROUBLESHOOTING
═════════════════════════════════════════════════════════════════════

Issue: Cannot connect to backend
Solution: Check backend is running on localhost:5000
         Run: python app.py in terminal

Issue: npm install fails
Solution: Update npm: npm install -g npm@latest
          Delete node_modules and try again

Issue: Location permission denied
Solution: Enable location in browser settings
         On mobile, check app permissions

Issue: Blank page
Solution: Open browser console (F12)
         Check for error messages
         Verify .env file exists


🎓 NEXT STEPS
═════════════════════════════════════════════════════════════════════

1. ✅ Install dependencies: npm install
2. ✅ Create .env file
3. ✅ Start backend: python app.py
4. ✅ Start frontend: npm run dev
5. ✅ Open http://localhost:3000
6. ✅ Register a test account
7. ✅ Test all features


═════════════════════════════════════════════════════════════════════
              ✅ FRONTEND READY TO USE! 🚀
         Start with: cd frontend && npm install && npm run dev
═════════════════════════════════════════════════════════════════════
